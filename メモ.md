## することリスト
- トップのヒーロー用動画をもらう
- もらってHero.jsに差し込む
    /home/t8s/MyDocker/mtok/mtokhp_Nextjs/src/components/Hero.js
- レジンテーブル事業の文言変更
- 不動産事業の文言変更
- 不動産事業のトップ画像変更
- 全体のフォントは希望があれば変更
- レジンギャラリー用画像6枚
    自動でインスタから引っ張ってくる方法はないか調査
- すべて完了したらVercelからAWS EC2に変更
- ドメイン周りの設定変更、Wixから移設かAWSで新しく発行するか聞く
- .envをMacに共有する


## 本番向け作業

- Docker設定を本番向けに変更
    Dockerfileがnpm run devで実行されているが、本番環境ではnpm run build && npm startを使う方が早い
- docker-compose.ymlを本番向けに変更
    docker-compose.yml:12でNODE_ENV=developmentに設定されているのを本番ではNODE_ENV=productionにする

## その他改善案
- スクロール時の動的演出
    現在：固定的な表示
    <strong>提案：</strong> react-intersection-observerとframer-motion（既にインストール済み）を使用
    - パラレックス効果：スクロールに応じて背景画像が異なるスピードで動く
    - スティッキーセクション：セクションのテキストが固定表示されながらスクロール
    - 段階的フェードイン：コンテンツが画面に入るたびに自動アニメーション
    ``` javascript
    // 例：Gallery のホバーで拡大 + スクロール検出
    <motion.div
    initial={{ opacity: 0, scale: 0.9 }}
    whileInView={{ opacity: 1, scale: 1 }}
    whileHover={{ scale: 1.05 }}
    transition={{ duration: 0.3 }}
    >
    ```

- Gallery.js高速化 + ホバー演出
    - Lightbox機能：画像クリックで拡大表示（react-lightbox-component など）
    - ホバー時のテキストオーバーレイ：商品名・説明が浮かび上がる
    - Next/Image の最適化：画像は WebP で配信、遅延読込み
    - Swiper(インストール済み)で、カルーセル表示オプション

- ブランドカラーの統一
    現在：青（blue-800）とグレーが混在
    <strong>提案：</strong>
    ``` css
    /* 新しいカラーパレット（杉/檜のイメージ） */
    --color-primary: #2d5016 /* 濃い緑(杉) */
    --color-secondary: #8b7355 /* 木目 */
    --color-accent: #d4a574 /* 明るい木色 */
    --color-dark: #1a1a1a /* 深い黒 */
    ```

- 各ページのTopコンポーネントのフォントだけ和風なフォントに変える。
    googleフォントで使えるおしゃれなものを調べる。
    