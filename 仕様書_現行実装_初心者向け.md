# 株式会社マツオカサイト 現行実装仕様書（初心者向け）

## 1. この資料の目的
この資料は、今あるソースコードが何をしているかを、エンジニア初心者でも追えるように整理したものです。

- どの画面が、どのファイルで作られているか
- 管理画面で更新した内容が、どこに保存されるか
- APIが何を受け取り、何を返すか
- 変更時にまず見るべき場所

を中心に説明します。

注意:
- 既存の設計資料（仕様書_設計書.md）はありますが、現行実装との差分があるため、この資料は「現在のコード基準」で記載しています。

---

## 2. 全体像（最初にここだけ読めばOK）
このサイトは Next.js（App Router）で作られたコーポレートサイトです。

- 公開ページ
  - トップ
  - 会社情報
  - 各事業ページ
  - 採用
  - Forestページ
- 管理ページ
  - /admin/login でログイン
  - /admin で NEWS とレジンテーブル画像を更新
- データ保存
  - 基本は src/data 配下の JSON
  - 環境によって Vercel Blob またはメモリに切り替え

---

## 3. ディレクトリ役割

### 3-1. 画面
- src/app
  - URLに対応するページ本体（page.js）
  - APIルート（api/.../route.js）
  - 全ページ共通レイアウト（layout.js）

### 3-2. 再利用UI
- src/components
  - ヘッダー、問い合わせ、ニュース表示などの共通部品

### 3-3. データ
- src/data/site-content.json
  - NEWS、レジンテーブルギャラリー、造船実績など
- src/data/analytics.json
  - ページごとのGA設定

### 3-4. データ操作ロジック
- src/lib/adminContent.js
  - JSON読込・保存
  - 保存先（file/blob/memory）切替
  - 画像アップロード補助

### 3-5. 静的アセット
- public/images
- public/videos

---

## 4. ルーティング一覧

### 4-1. 公開ページ
- / : src/app/page.js
- /about : src/app/about/page.js
- /business/construction : src/app/business/construction/page.js
- /business/shipbuilding : src/app/business/shipbuilding/page.js
- /business/resin-table : src/app/business/resin-table/page.js
- /business/real-estate : src/app/business/real-estate/page.js
- /recruit : src/app/recruit/page.js
- /forest : src/app/forest/page.js

### 4-2. 管理ページ
- /admin/login : src/app/admin/login/page.js
- /admin : src/app/admin/page.js

### 4-3. API
- /api/admin/login
- /api/admin/logout
- /api/admin/config
- /api/admin/gallery
- /api/public/analytics
- /api/public/content

---

## 5. 画面ごとの担当

### 5-1. 全ページ共通
- src/app/layout.js
  - フォント設定（Noto Sans/Serif JP）
  - Header表示
  - GoogleAnalytics読み込み

### 5-2. トップページ
- src/app/page.js
  - Hero
  - News
  - CardSection
  - resin
  - InstagramFeed
  - Contact

補足:
- News はサーバー側で site-content.json を読み、表示期間や有効フラグで表示可否を判定

### 5-3. 会社情報ページ
- src/app/about/page.js
  - CompanyOverview
  - History
  - OfficerIntroduction
  - Concept / Features は現在コメントアウト

### 5-4. 事業ページ
- construction: Top + ConstructionStrengths + ProductIntroduction
- shipbuilding: Top + Strengths + ProductIntroduction + Achievements
- resin-table: Hero + ResinTableStrengths + Gallery
- real-estate: Top + RealEstateStrengths（ProductIntroductionはコメントアウト）

### 5-5. 採用ページ
- src/app/recruit/page.js
  - 現在は Menu/Merit/Engage がコメントアウトされ、実質空表示

### 5-6. Forestページ
- src/app/forest/page.js
  - ページ内で Header を再度描画している（共通layoutにもHeaderがあるため二重表示の可能性）

---

## 6. 管理機能の流れ

### 6-1. ログイン
1. /admin/login でユーザー名とパスワードを入力
2. /api/admin/login へ POST
3. 認証成功時、admin_session Cookie 発行
4. /admin に遷移

### 6-2. 管理画面でできること
- NEWSの表示ON/OFF
- NEWS本文、日付、URL、表示期間の編集
- NEWS画像の添付
- レジンテーブルのギャラリー9枠のうち任意枠を更新
- ログアウト

### 6-3. 保存先
- 保存APIは src/lib/adminContent.js を経由
- 保存モードは以下を自動判定
  - file: src/data の JSON を更新
  - blob: Vercel Blob に保存
  - memory: プロセスメモリ（再起動で消える）

---

## 7. API仕様（簡易）

### 7-1. /api/admin/login
- Method: POST
- Input(JSON): username, password
- Output:
  - 成功: { ok: true } + Cookie設定
  - 失敗: 401

### 7-2. /api/admin/logout
- Method: POST
- Output: { ok: true } + Cookie削除

### 7-3. /api/admin/config
- Method: GET
  - 認証必須
  - analytics/content を返す用途
- Method: POST
  - 認証必須
  - multipart/form-data: NEWS更新
  - application/json: analytics更新

### 7-4. /api/admin/gallery
- Method: POST
- 認証必須
- Input(form-data): image, slotIndex
- 役割: レジンテーブルの9枠ギャラリー画像更新

### 7-5. /api/public/analytics
- Method: GET
- 役割: GA設定の公開取得

### 7-6. /api/public/content
- Method: GET
- 役割: 公開コンテンツ取得

---

## 8. データ構造（重要）

### 8-1. site-content.json の主なキー
- resinTable.heroImage
- resinTable.galleryImages（配列、想定9件）
- shipbuilding.achievements（title, imageUrl）
- news
  - enabled
  - item.date
  - item.title
  - item.body
  - item.url
  - item.hasImage
  - item.imageUrl
  - item.startDate
  - item.endDate

### 8-2. analytics.json の主なキー
- pages（配列）
  - path
  - title
  - enabled
  - measurementId

---

## 9. 環境変数（名前だけ覚えればOK）
値そのものは秘密情報なのでここでは書きません。

- 管理ログイン関連
  - ADMIN_USERNAME
  - ADMIN_PASSWORD
  - ADMIN_SESSION_TOKEN
- 旧互換/別認証関連
  - BASIC_AUTH_USER
  - BASIC_AUTH_PASS
  - BASIC_AUTH_TOKEN
- Blob保存関連
  - BLOB_READ_WRITE_TOKEN
  - BLOB_TOKEN
  - BLOB_STORE_ID
  - VERCEL_OIDC_TOKEN
- フロント公開値
  - NEXT_PUBLIC_GA_ID
  - NEXT_PUBLIC_FORMSPREE_URL

---

## 10. 開発・起動方法

### 10-1. ローカル起動
- npm install
- npm run dev
- http://localhost:3000 を開く

### 10-2. Docker起動
- docker-compose up --build

補足:
- Dockerfile は npm run dev を起動する開発用寄り構成

---

## 11. 変更時のガイド（初心者向け）

### 11-1. どこを触れば何が変わるか
- トップページの並び変更: src/app/page.js
- メニュー変更: src/components/Header.js
- NEWS表示ロジック変更: src/components/News.js
- 管理画面の入力項目変更: src/app/admin/AdminDashboard.js
- 管理API変更: src/app/api/admin/config/route.js, src/app/api/admin/gallery/route.js
- 保存先ロジック変更: src/lib/adminContent.js

### 11-2. 先に確認すること
- 管理画面で保存した値が site-content.json に反映されるか
- NEWS表示期間の判定が想定どおりか
- 画像URLが Next.js の画像設定（next.config.mjs）で許可されているか

---

## 12. 既知課題（現行ソース基準）

1. APIとデータ層に重複実装が混在
- src/app/api/admin/config/route.js
- src/app/api/admin/gallery/route.js
- src/lib/adminContent.js

同じ関数名（GET/POST/isAuthorizedなど）の定義が複数連結されており、将来の保守で意図しない挙動を生みやすい状態です。

2. 認証ロジックが複数思想で混在
- admin_sessionベース
- production時のみ緩いヘッダ判定

3. /forest で Headerを個別描画
- layout.js 側にも Header があるため重複表示の可能性

4. /recruit は主要コンポーネントがコメントアウト
- 画面が空に近い状態

5. 秘密情報の扱い注意
- .env の管理情報とBlob情報は漏えいリスクが高い
- 本番運用では値ローテーションとシークレット管理が必要

---

## 13. 次にやると保守が楽になる改善

優先度 高
- admin APIとadminContentの重複実装を1系統に整理
- 認証判定を1方式に統一

優先度 中
- ForestページのHeader重複を解消
- Recruitページのコンテンツ復活または仕様確定

優先度 低
- 既存仕様書（仕様書_設計書.md）を現行コードへ同期

---

以上。
この資料は、まず「どこを見るべきか」を明確にすることを目的にしています。